{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.13.4/.schema/devbox.schema.json",
  "packages": ["go@latest", "opencv@latest", "gdb@latest"],
  "env": {
    "PKG_CONFIG_PATH": "$DEVBOX_PACKAGES_DIR/lib/pkgconfig",
    "GOPATH": "${PWD}/.devbox/virtenv/go",
    "GOBIN": "${PWD}/.devbox/virtenv/go/bin",
    "PATH": "${GOBIN}:${PATH}"
  },
  "shell": {
    "init_hook": [
      "echo 'Welcome to devbox!' > /dev/null",
      "export GOMODCACHE=${PWD}/.devbox/virtenv/go/pkg/mod",
      "export GOCACHE=${PWD}/.devbox/virtenv/go/cache",
      "go install github.com/go-delve/delve/cmd/dlv@latest"
    ],
    "scripts": {
      "test": ["echo \"Error: no test specified\" && exit 1"]
    }
  }
}
